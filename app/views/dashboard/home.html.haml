%h1 Robinhood Dashboard
- if session[:robinhood_auth_token].present?
  = link_to "Log out", logout_path
  %p Here you can find information about your investments. Click on an investment for more information.
  #basic-info{data: {behavior: "DeferredLoader", path: basic_info_path}}
  #portfolios{data: {behavior: "DeferredLoader", path: portfolios_path}}
- else
  = render "layouts/robinhood_login_form"