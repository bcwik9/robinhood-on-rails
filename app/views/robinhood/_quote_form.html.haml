#quote-form
  = form_tag quote_path, method: :get do
    .form-group
      = text_field_tag :symbols, nil, placeholder: "Search stock or name", class: "form-control"
      %span.fa.fa-search
    = submit_tag "Get quote", class: "btn btn-default"